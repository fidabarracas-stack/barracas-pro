services:
  - type: web
    name: barracas-pro
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: cd backend && python -m uvicorn main:app --host 0.0.0.0 --port $PORT --root-path /
    envVars:
      - key: SECRET_KEY
        generateValue: true
